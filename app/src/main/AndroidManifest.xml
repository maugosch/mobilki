<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sony.androidproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlynnoscFinansowa" />
        <activity android:name=".SprawnoscDzialania" />
        <activity android:name=".WskaznikiGieldowe" />
        <activity android:name=".WskaznikWyplacalnosci" />
        <activity android:name=".WskaznikRentownosci" />
        <activity android:name=".Modele" />
        <activity android:name=".ModelAltman" />
        <activity android:name=".ModelHolda" />
        <activity android:name=".MainMainActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PlynnoscFinansowa2" />
        <activity
            android:name=".WskaznikRentownosci2"
            android:label="@string/title_activity_wskaznik_rentownosci2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".WskaznikiGieldowe2" />
        <activity android:name=".WskaznikWyplacalnosci2" />
        <activity android:name=".SprawnoscDzialania2"></activity>
    </application>

</manifest>